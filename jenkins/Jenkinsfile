pipeline {
    agent any
    stages {
        stage('Build'){
            steps {
                sh 'docker build -f docker/test-dockerfile-v1 -t chat-test .'
                sh 'docker run chat-test'

            }
        }
    }
}